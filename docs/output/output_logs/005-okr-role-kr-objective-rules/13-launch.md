# STEP 13 - Launch

## Goal
Build backend + frontend and open working UI.

## Environment blocker
Current terminal does not have Node.js/npm, so runtime launch command cannot be executed.

## Launch commands to run locally
1. `cd backend && npm install && npm run start:dev`
2. `cd frontend && npm install && npm run dev`
3. Open http://localhost:5173

## Verdict
BLOCKED BY TOOLCHAIN IN TERMINAL
